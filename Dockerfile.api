# syntax=docker/dockerfile:1
FROM node:20-alpine
WORKDIR /app
ENV NODE_ENV=production
COPY package*.json ./
RUN npm ci --omit=dev
COPY server ./server
COPY db ./db
EXPOSE 4600
CMD ["sh", "-c", "API_PORT=${PORT:-4600} node server/index.js"]
